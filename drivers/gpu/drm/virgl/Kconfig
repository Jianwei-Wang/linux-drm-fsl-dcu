config DRM_VIRGL
	tristate "QEMU Virtual 3D GPU"
	depends on DRM && PCI
	select FB_SYS_FILLRECT
	select FB_SYS_COPYAREA
	select FB_SYS_IMAGEBLIT
        select DRM_KMS_HELPER
        select DRM_TTM
	help
	   QEMU based 3D GPU.